# Copy to api/.env and fill in. api/.env is gitignored — never commit a key.
ANTHROPIC_API_KEY=sk-ant-...

# Optional. Defaults to the docker-compose database on host port 5433.
# DATABASE_URL=postgresql://ieeecs:ieeecs@localhost:5433/resume_kit
